{
  "name": "agilehack",
  "description": "AgileHack hackaton spike!",
  "version": "0.0.1",
  "scripts": {
	  "prestart" : "npm install && cd public && npm install",
	  "start": "node server.js"
  },
  "dependencies": {
    "express": "*",
    "socket.io": "*",
    "chat.io": "*",
    "bower": "*",
    "ejs": "*"
  }
}
